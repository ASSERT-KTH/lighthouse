[package]
name = "verclient3"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "=1.2.1", default-features = false, features = ['std'] }
blst = { version = "0.3.3" }
