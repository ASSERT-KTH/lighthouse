[package]
name = "hello_guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "=1.1.3", default-features = false, features = ['std'] }
safe_arith = { path = "../../consensus/safe_arith" }
nalgebra = "0.32"
